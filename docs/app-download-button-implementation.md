# App download button implementation

Visible mobile-app download entry points are now present on both the signed-out login screen and the authenticated portal header. They open the public `/app-download.html` landing page.

The page deliberately does not expose a broken APK link. It states that the installable Android package is still pending signing/publication and is ready to become the stable installer landing page when that binary is published.
